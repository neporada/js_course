function hi_is_function(args){
    console.log("");
};

function showMessage(){
    alert("");
}
var showMessageElse = showMessage;

function getAge(age, name) {
    alert(age);
}

getAge(13, "Denis");

console.log(gage, name);

// getAge(13, 13) - выполнение функции, передача ей параметров;
// gatAge({
//      name: "", 
//      age: 13
//}) - тоже самое;

function getName() {
    var name = prompt("Enter yor name: ");
    return prompt;
}

getName();
var myName = getName();


function getData() {
    var myName = prompt("Enter yor name: ");
    var myAge = prompt("Enter yor age: ");

    return {name: myName, age: myAge};
}

getData();